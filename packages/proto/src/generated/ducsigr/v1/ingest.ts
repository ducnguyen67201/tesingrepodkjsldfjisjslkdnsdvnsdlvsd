// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: ducsigr/v1/ingest.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../google/protobuf/struct";
import { Timestamp } from "../../google/protobuf/timestamp";
import { SpanLevel, spanLevelFromJSON, spanLevelToJSON, spanLevelToNumber, TokenUsage } from "./common";

/** User information for tracking end-users */
export interface UserInfo {
  name?: string | undefined;
  email?: string | undefined;
  metadata?: { [key: string]: any } | undefined;
}

/** Request to ingest a trace with spans */
export interface IngestTraceRequest {
  /** Trace information */
  traceId?: string | undefined;
  name: string;
  metadata?:
    | { [key: string]: any }
    | undefined;
  /** Spans to ingest */
  spans: IngestSpan[];
  /** Session tracking (for multi-turn conversations) */
  sessionId?:
    | string
    | undefined;
  /** User tracking (end-users of AI applications) */
  userId?: string | undefined;
  user?: UserInfo | undefined;
}

/** Span data for ingestion */
export interface IngestSpan {
  /** Optional, server generates if not provided */
  spanId?: string | undefined;
  parentSpanId?: string | undefined;
  name: string;
  startTime?: Date | undefined;
  endTime?: Date | undefined;
  input?: { [key: string]: any } | undefined;
  output?: { [key: string]: any } | undefined;
  metadata?: { [key: string]: any } | undefined;
  model?: string | undefined;
  modelParameters?: { [key: string]: any } | undefined;
  usage?: TokenUsage | undefined;
  level: SpanLevel;
  statusMessage?: string | undefined;
}

/** Response after ingesting a trace */
export interface IngestTraceResponse {
  traceId: string;
  spanIds: string[];
  success: boolean;
}

/** Batch ingestion request */
export interface IngestBatchRequest {
  traces: IngestTraceRequest[];
}

/** Batch ingestion response */
export interface IngestBatchResponse {
  results: IngestTraceResponse[];
  successCount: number;
  errorCount: number;
}

/** Health check */
export interface HealthRequest {
}

export interface HealthResponse {
  status: string;
  version: string;
}

function createBaseUserInfo(): UserInfo {
  return { name: undefined, email: undefined, metadata: undefined };
}

export const UserInfo: MessageFns<UserInfo> = {
  encode(message: UserInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined) {
      writer.uint32(10).string(message.name);
    }
    if (message.email !== undefined) {
      writer.uint32(18).string(message.email);
    }
    if (message.metadata !== undefined) {
      Struct.encode(Struct.wrap(message.metadata), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.metadata = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      email: isSet(object.email) ? globalThis.String(object.email) : undefined,
      metadata: isObject(object.metadata) ? object.metadata : undefined,
    };
  },

  toJSON(message: UserInfo): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.email !== undefined) {
      obj.email = message.email;
    }
    if (message.metadata !== undefined) {
      obj.metadata = message.metadata;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserInfo>, I>>(base?: I): UserInfo {
    return UserInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserInfo>, I>>(object: I): UserInfo {
    const message = createBaseUserInfo();
    message.name = object.name ?? undefined;
    message.email = object.email ?? undefined;
    message.metadata = object.metadata ?? undefined;
    return message;
  },
};

function createBaseIngestTraceRequest(): IngestTraceRequest {
  return {
    traceId: undefined,
    name: "",
    metadata: undefined,
    spans: [],
    sessionId: undefined,
    userId: undefined,
    user: undefined,
  };
}

export const IngestTraceRequest: MessageFns<IngestTraceRequest> = {
  encode(message: IngestTraceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.traceId !== undefined) {
      writer.uint32(10).string(message.traceId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.metadata !== undefined) {
      Struct.encode(Struct.wrap(message.metadata), writer.uint32(26).fork()).join();
    }
    for (const v of message.spans) {
      IngestSpan.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.sessionId !== undefined) {
      writer.uint32(42).string(message.sessionId);
    }
    if (message.userId !== undefined) {
      writer.uint32(50).string(message.userId);
    }
    if (message.user !== undefined) {
      UserInfo.encode(message.user, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestTraceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestTraceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.metadata = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.spans.push(IngestSpan.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.user = UserInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestTraceRequest {
    return {
      traceId: isSet(object.traceId) ? globalThis.String(object.traceId) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      metadata: isObject(object.metadata) ? object.metadata : undefined,
      spans: globalThis.Array.isArray(object?.spans) ? object.spans.map((e: any) => IngestSpan.fromJSON(e)) : [],
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
      user: isSet(object.user) ? UserInfo.fromJSON(object.user) : undefined,
    };
  },

  toJSON(message: IngestTraceRequest): unknown {
    const obj: any = {};
    if (message.traceId !== undefined) {
      obj.traceId = message.traceId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.metadata !== undefined) {
      obj.metadata = message.metadata;
    }
    if (message.spans?.length) {
      obj.spans = message.spans.map((e) => IngestSpan.toJSON(e));
    }
    if (message.sessionId !== undefined) {
      obj.sessionId = message.sessionId;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    if (message.user !== undefined) {
      obj.user = UserInfo.toJSON(message.user);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestTraceRequest>, I>>(base?: I): IngestTraceRequest {
    return IngestTraceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestTraceRequest>, I>>(object: I): IngestTraceRequest {
    const message = createBaseIngestTraceRequest();
    message.traceId = object.traceId ?? undefined;
    message.name = object.name ?? "";
    message.metadata = object.metadata ?? undefined;
    message.spans = object.spans?.map((e) => IngestSpan.fromPartial(e)) || [];
    message.sessionId = object.sessionId ?? undefined;
    message.userId = object.userId ?? undefined;
    message.user = (object.user !== undefined && object.user !== null) ? UserInfo.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseIngestSpan(): IngestSpan {
  return {
    spanId: undefined,
    parentSpanId: undefined,
    name: "",
    startTime: undefined,
    endTime: undefined,
    input: undefined,
    output: undefined,
    metadata: undefined,
    model: undefined,
    modelParameters: undefined,
    usage: undefined,
    level: SpanLevel.SPAN_LEVEL_UNSPECIFIED,
    statusMessage: undefined,
  };
}

export const IngestSpan: MessageFns<IngestSpan> = {
  encode(message: IngestSpan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.spanId !== undefined) {
      writer.uint32(10).string(message.spanId);
    }
    if (message.parentSpanId !== undefined) {
      writer.uint32(18).string(message.parentSpanId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.startTime !== undefined) {
      Timestamp.encode(toTimestamp(message.startTime), writer.uint32(34).fork()).join();
    }
    if (message.endTime !== undefined) {
      Timestamp.encode(toTimestamp(message.endTime), writer.uint32(42).fork()).join();
    }
    if (message.input !== undefined) {
      Struct.encode(Struct.wrap(message.input), writer.uint32(50).fork()).join();
    }
    if (message.output !== undefined) {
      Struct.encode(Struct.wrap(message.output), writer.uint32(58).fork()).join();
    }
    if (message.metadata !== undefined) {
      Struct.encode(Struct.wrap(message.metadata), writer.uint32(66).fork()).join();
    }
    if (message.model !== undefined) {
      writer.uint32(74).string(message.model);
    }
    if (message.modelParameters !== undefined) {
      Struct.encode(Struct.wrap(message.modelParameters), writer.uint32(82).fork()).join();
    }
    if (message.usage !== undefined) {
      TokenUsage.encode(message.usage, writer.uint32(90).fork()).join();
    }
    if (message.level !== SpanLevel.SPAN_LEVEL_UNSPECIFIED) {
      writer.uint32(96).int32(spanLevelToNumber(message.level));
    }
    if (message.statusMessage !== undefined) {
      writer.uint32(106).string(message.statusMessage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestSpan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestSpan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.spanId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.parentSpanId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.input = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.output = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.metadata = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.model = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.modelParameters = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.usage = TokenUsage.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.level = spanLevelFromJSON(reader.int32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.statusMessage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestSpan {
    return {
      spanId: isSet(object.spanId) ? globalThis.String(object.spanId) : undefined,
      parentSpanId: isSet(object.parentSpanId) ? globalThis.String(object.parentSpanId) : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      startTime: isSet(object.startTime) ? fromJsonTimestamp(object.startTime) : undefined,
      endTime: isSet(object.endTime) ? fromJsonTimestamp(object.endTime) : undefined,
      input: isObject(object.input) ? object.input : undefined,
      output: isObject(object.output) ? object.output : undefined,
      metadata: isObject(object.metadata) ? object.metadata : undefined,
      model: isSet(object.model) ? globalThis.String(object.model) : undefined,
      modelParameters: isObject(object.modelParameters) ? object.modelParameters : undefined,
      usage: isSet(object.usage) ? TokenUsage.fromJSON(object.usage) : undefined,
      level: isSet(object.level) ? spanLevelFromJSON(object.level) : SpanLevel.SPAN_LEVEL_UNSPECIFIED,
      statusMessage: isSet(object.statusMessage) ? globalThis.String(object.statusMessage) : undefined,
    };
  },

  toJSON(message: IngestSpan): unknown {
    const obj: any = {};
    if (message.spanId !== undefined) {
      obj.spanId = message.spanId;
    }
    if (message.parentSpanId !== undefined) {
      obj.parentSpanId = message.parentSpanId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.startTime !== undefined) {
      obj.startTime = message.startTime.toISOString();
    }
    if (message.endTime !== undefined) {
      obj.endTime = message.endTime.toISOString();
    }
    if (message.input !== undefined) {
      obj.input = message.input;
    }
    if (message.output !== undefined) {
      obj.output = message.output;
    }
    if (message.metadata !== undefined) {
      obj.metadata = message.metadata;
    }
    if (message.model !== undefined) {
      obj.model = message.model;
    }
    if (message.modelParameters !== undefined) {
      obj.modelParameters = message.modelParameters;
    }
    if (message.usage !== undefined) {
      obj.usage = TokenUsage.toJSON(message.usage);
    }
    if (message.level !== SpanLevel.SPAN_LEVEL_UNSPECIFIED) {
      obj.level = spanLevelToJSON(message.level);
    }
    if (message.statusMessage !== undefined) {
      obj.statusMessage = message.statusMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestSpan>, I>>(base?: I): IngestSpan {
    return IngestSpan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestSpan>, I>>(object: I): IngestSpan {
    const message = createBaseIngestSpan();
    message.spanId = object.spanId ?? undefined;
    message.parentSpanId = object.parentSpanId ?? undefined;
    message.name = object.name ?? "";
    message.startTime = object.startTime ?? undefined;
    message.endTime = object.endTime ?? undefined;
    message.input = object.input ?? undefined;
    message.output = object.output ?? undefined;
    message.metadata = object.metadata ?? undefined;
    message.model = object.model ?? undefined;
    message.modelParameters = object.modelParameters ?? undefined;
    message.usage = (object.usage !== undefined && object.usage !== null)
      ? TokenUsage.fromPartial(object.usage)
      : undefined;
    message.level = object.level ?? SpanLevel.SPAN_LEVEL_UNSPECIFIED;
    message.statusMessage = object.statusMessage ?? undefined;
    return message;
  },
};

function createBaseIngestTraceResponse(): IngestTraceResponse {
  return { traceId: "", spanIds: [], success: false };
}

export const IngestTraceResponse: MessageFns<IngestTraceResponse> = {
  encode(message: IngestTraceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.traceId !== "") {
      writer.uint32(10).string(message.traceId);
    }
    for (const v of message.spanIds) {
      writer.uint32(18).string(v!);
    }
    if (message.success !== false) {
      writer.uint32(24).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestTraceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestTraceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.spanIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestTraceResponse {
    return {
      traceId: isSet(object.traceId) ? globalThis.String(object.traceId) : "",
      spanIds: globalThis.Array.isArray(object?.spanIds) ? object.spanIds.map((e: any) => globalThis.String(e)) : [],
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
    };
  },

  toJSON(message: IngestTraceResponse): unknown {
    const obj: any = {};
    if (message.traceId !== "") {
      obj.traceId = message.traceId;
    }
    if (message.spanIds?.length) {
      obj.spanIds = message.spanIds;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestTraceResponse>, I>>(base?: I): IngestTraceResponse {
    return IngestTraceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestTraceResponse>, I>>(object: I): IngestTraceResponse {
    const message = createBaseIngestTraceResponse();
    message.traceId = object.traceId ?? "";
    message.spanIds = object.spanIds?.map((e) => e) || [];
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseIngestBatchRequest(): IngestBatchRequest {
  return { traces: [] };
}

export const IngestBatchRequest: MessageFns<IngestBatchRequest> = {
  encode(message: IngestBatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.traces) {
      IngestTraceRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestBatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestBatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.traces.push(IngestTraceRequest.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestBatchRequest {
    return {
      traces: globalThis.Array.isArray(object?.traces)
        ? object.traces.map((e: any) => IngestTraceRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: IngestBatchRequest): unknown {
    const obj: any = {};
    if (message.traces?.length) {
      obj.traces = message.traces.map((e) => IngestTraceRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestBatchRequest>, I>>(base?: I): IngestBatchRequest {
    return IngestBatchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestBatchRequest>, I>>(object: I): IngestBatchRequest {
    const message = createBaseIngestBatchRequest();
    message.traces = object.traces?.map((e) => IngestTraceRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIngestBatchResponse(): IngestBatchResponse {
  return { results: [], successCount: 0, errorCount: 0 };
}

export const IngestBatchResponse: MessageFns<IngestBatchResponse> = {
  encode(message: IngestBatchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      IngestTraceResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.successCount !== 0) {
      writer.uint32(16).int32(message.successCount);
    }
    if (message.errorCount !== 0) {
      writer.uint32(24).int32(message.errorCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestBatchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestBatchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(IngestTraceResponse.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.successCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.errorCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestBatchResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => IngestTraceResponse.fromJSON(e))
        : [],
      successCount: isSet(object.successCount) ? globalThis.Number(object.successCount) : 0,
      errorCount: isSet(object.errorCount) ? globalThis.Number(object.errorCount) : 0,
    };
  },

  toJSON(message: IngestBatchResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => IngestTraceResponse.toJSON(e));
    }
    if (message.successCount !== 0) {
      obj.successCount = Math.round(message.successCount);
    }
    if (message.errorCount !== 0) {
      obj.errorCount = Math.round(message.errorCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestBatchResponse>, I>>(base?: I): IngestBatchResponse {
    return IngestBatchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestBatchResponse>, I>>(object: I): IngestBatchResponse {
    const message = createBaseIngestBatchResponse();
    message.results = object.results?.map((e) => IngestTraceResponse.fromPartial(e)) || [];
    message.successCount = object.successCount ?? 0;
    message.errorCount = object.errorCount ?? 0;
    return message;
  },
};

function createBaseHealthRequest(): HealthRequest {
  return {};
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(_: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HealthRequest {
    return {};
  },

  toJSON(_: HealthRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthRequest>, I>>(base?: I): HealthRequest {
    return HealthRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthRequest>, I>>(_: I): HealthRequest {
    const message = createBaseHealthRequest();
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return { status: "", version: "" };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthResponse {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthResponse>, I>>(base?: I): HealthResponse {
    return HealthResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthResponse>, I>>(object: I): HealthResponse {
    const message = createBaseHealthResponse();
    message.status = object.status ?? "";
    message.version = object.version ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
