# Use hoisted node_modules structure (flat, like npm) to ensure
# @prisma/client is resolvable from generated Prisma code in @ducsigr/db
# This is required because Prisma 7's generated code imports
# @prisma/client/runtime/client which pnpm's default symlinked structure can't resolve
node-linker=hoisted
shamefully-hoist=true
