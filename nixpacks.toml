[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
dependsOn = ["install"]
cmds = [
    "cd packages/db && npx prisma generate --schema prisma/schema",
    "pnpm build"
]

[start]
cmd = "pnpm start"
